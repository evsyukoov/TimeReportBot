package stateMachine;

public enum State {
    REGISTER_NAME,
    REGISTER_DEPARTMENT,
    REGISTER_POSITION,
    CHOOSE_DAY,
    TODAY,
    SELECT_DATE,
    SELECT_PROJECT,
    ADD_PROJECT,
    FINISH
}
